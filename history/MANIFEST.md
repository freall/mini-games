# 版本台账 · 小游戏乐园

| 版本 | 时间 | 变更摘要 | 交付方式 |
| --- | --- | --- | --- |
| v1 | 2026-09-13 | 首版：Canvas 回转寿司街机射击游戏。含酱油炮台瞄准射击、红/蓝/银/金四档寿司盘 + 绿茶弹药补给 + 芥末炸弹（误中扣命）、顾客订单系统（击中所需寿司送菜得大奖、耐心耗尽扣命）、连击倍率（最高 ×8）、酱油弹药管理、等级递进加速、和风居酒屋场景（青海波后墙/暖帘/灯笼/双层传送带/吧台顾客）、Web Audio 合成音效、开始/暂停/结算三态、最高分本地保存、桌面鼠标键盘 + 移动端触控双适配 | 单文件URL版 |
| v2 | 2026-09-14 | 重构为单入口「小游戏乐园.html」多视图架构（门户 / 回转寿司 / AI图标消消乐）。①新增 AI图标消消乐：8×8 三消棋盘、10 款主流 AI 工具图标（ChatGPT/Gemini/通义千问/DeepSeek/Copilot/Perplexity/豆包/文心一言/Kimi/Grok），图标联网加载 + 品牌配色徽章离线兜底，交换消除/下落补位/连锁倍率、四连生成直线清除+五连生成全色清除特殊图标、点击引爆、关卡目标分与步数限制、死局自动重排、最高分与关卡进度本地保存、Web Audio 合成音效、桌面点选拖动 + 移动端滑动双适配。②新增门户首页：双游戏封面卡片（消消乐封面用同源图标渲染）、最高分与关卡进度回显、点击进入、各游戏内返回乐园。③回转寿司改为可挂载模块、样式作用域隔离、新增返回乐园入口，玩法与数值不变。核心三消逻辑经 25 项无头单元测试、8 脚本语法校验、DOM id 交叉核对全通过 | 单文件URL版 |
| v3 | 2026-09-14 | ①**GitHub Pages 从单页改为多页面结构**（单页扩展性差：三视图塞一个文件、样式互相污染、无法按页加载、分享链接只能指向同一 URL）：门户 `index.html` + `sushi/` + `match3/` 各自独立页面，共享 `assets/`，全站相对路径，门户卡片导航 + 游戏页返回乐园 + Escape 回门户由 `build-pages.mjs` 与 `site-src/assets/boot.js` 接管。②构建/发布工具链补齐并用 `games.config.mjs` 做游戏清单单一来源：`build-pages.mjs`（多页站点）、`build-single.mjs`（自包含单文件交付版，此前一直是手工拼的）、`publish.mjs`（一条命令构建+推 gh-pages，用临时 GIT_INDEX_FILE + commit-tree，不切分支不动工作区）、`verify-site.mjs`（结构自检 + `--selftest` 注入测试，4/4 类缺陷可捕获）。③**修复 P0**：消消乐开始界面被 `overflow:hidden` 裁剪——开局前 `board` 为 null 导致 `resize()` 提前 return、`.m3-board` 高度 0 → `.m3-boardwrap` 塌成 22px，开始/暂停/结算覆盖层（inset:0 绝对定位在 boardwrap 内）整个被裁掉，「开始挑战」按钮不可见也点不到，游戏实际无法开局；给 `.m3-board` 加 `aspect-ratio:1/1` 兜底后修复。④`.view` 显隐改用 `!important`（ID 选择器的 display:flex 权重高于 `.view{display:none}`，否则三视图同时堆叠显示）。⑤端到端验证：Playwright 无头浏览器 17 项断言全绿（门户卡片跳转 / 寿司 canvas 绘制 / 消消乐 64 格+拖拽换位 / 返回与 Escape / 无控制台报错），单文件版 file:// 实测可玩 | 多页面站点 + 单文件URL版 |
| v4 | 2026-09-16 | **新增第三款游戏「深夜电台 · 摩尔斯电码」（Morse Radio）**：用电键把电文一字字发出去——短按出点、按住出划、松手停顿即字符结束（真实发报节奏，练的是手上功夫而非背表格）；按键时有持续侧音（sidetone）+ 信号灯 + 示波器式点划回显。①教学与记忆机制：36 个字符（A–Z、0–9）按 6 个教学组逐关解锁（E T → 高频字母 → 对称字母 → 进阶 → 补齐 → 数字），每关只多学一组；电文取自按已解锁字符过滤的真词库（SOS/TEAM/STONE…，保证一开始就能发出有意义的电报）；答错立刻回放正确码并给中文记忆口诀；错过的字符进「待复习」队列，后面几轮优先再考（词库没有含该字的短词时直接拼一个，保证复习真的发生）；每个字符记熟练度（对 +2 / 错 −1，≥6 算掌握），长期存本地并在门户显示「已掌握 N/36」。②趣味性：连击→信号强度 S1~S9、速度奖励、每字符节奏条、收听/看码随时可用但本字得分减半、电报音效全为 Web Audio 合成（含噪声干扰音）、暂停与结算面板用电台术语（QRT/QSL）。③架构：新增 `morse-data/core/morse` 三个脚本，核心逻辑与 DOM 完全分离（`tools/test-morse-core.mjs` 纯 node 77 项断言）；`games.config.mjs` 新增 `app` 字段、构建时写成 `<body data-app="xxx_APP">`，`boot.js` 改为按该属性挂载——**加游戏不用再改 boot.js**；自制工具链再次证明可用：本款游戏从零到上线**没改构建脚本与 boot.js 一行**，只是入口加视图 + 门户加卡片 + 配置加一条。④修掉三个自检/冒烟抓出来的真 bug：看码按钮因 `BY_CHAR` 取错模块而抛异常（页内报错）、一条电文发完后 450ms 的延迟重置会吞掉玩家抢先敲下的第一个符号、开始层主按钮被容器高度裁到可视区外点不到（覆盖层改 `position:fixed` 铺满视口修复）；另修正生命点 `'❤❤❤'.slice(0,n*2)` 长度算错导致扣命看不出来的显示 bug。⑤端到端：核心单测 77 项、多页面冒烟 30 项（新增 12 项摩尔斯断言，含真实电键时序发报）、单文件版冒烟 7 项，全部通过且连跑稳定，控制台零报错 | 多页面站点 + 单文件URL版 |
